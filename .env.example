# Get a free key at https://console.groq.com/keys (no card required).
# Without any key set, the app automatically falls back to an offline mock AI tutor.
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Gemini and Anthropic are also supported as alternatives - see README for details.
